<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Route | KL-Easy Trip</title>
    <%- include("partials/head") %>
</head>

<body class="bg-gray-100 text-gray-800">

    <!-- Navbar -->
    <%- include("partials/navbar") %>

        <section class="bg-green-800 text-white py-6 px-8">
            <div class="max-w-4xl mx-auto">
                <h1 class="text-2xl font-bold">Add New Route</h1>
            </div>
        </section>

        <section class="py-10 px-6 max-w-4xl mx-auto">
            <div class="bg-white p-8 rounded shadow">
                <h2 class="text-xl font-semibold mb-6">Route Details</h2>
                <form class="grid grid-cols-1 gap-4" action="/admin/add-routes" method="POST">
                    <input type="text" name="from" placeholder="From (e.g., Kochi)" class="border px-4 py-2 rounded"
                        required />
                    <input type="text" name="to" placeholder="To (e.g., Munnar)" class="border px-4 py-2 rounded"
                        required />
                    <input type="number" name="distanceKm" placeholder="Distance in KM"
                        class="border px-4 py-2 rounded" />
                    <input type="number" name="price" placeholder="Ticket Price ₹" class="border px-4 py-2 rounded"
                        required />
                    <input type="text" name="estimatedDuration" placeholder="Estimated Duration (e.g., 3h 45m)"
                        class="border px-4 py-2 rounded" />

                    <button type="submit" class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">
                        Add Route
                    </button>
                </form>
            </div>
        </section>

</body>

</html>